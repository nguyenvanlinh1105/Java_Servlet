package com.linhnguyen_16;

public class liThuyetFilter {
	/*
	 * Filter là gì ?
	 * Filter là một đối tượng dùng để xử lí request trước khi gọi đến một server đích, và respon trả về kết quả từ servlet.
	 * Dùng nhiều Filter một lúc
	 * Dễ dàng tích hợp bất cứ lúc nào
	 * 
	 * 
	 *  Tác dụng của filter 
	 *  Logging 
	 *  check ip
	 *  nén file
	 *  validate dữ liệu
	 */
}
